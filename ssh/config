{%@@ if os == "darwin" @@%}
Host *
    UseKeychain yes
{%@@ endif @@%}

{%@@ for item in hosts @@%}
Host {{@@ item @@}}
  HostName {{@@ hosts[item].host @@}}
  Port {{@@ hosts[item].port @@}}
  {%@@ if 'extra' in hosts[item] @@%}
{{@@ hosts[item].extra @@}}
  {%@@ endif @@%}
{%@@ endfor @@%}
